sudo: required
services:
  - docker

before_install:
  - docker build -t lakhanramnani/docker-react -f Dockerfile.dev .

script:
  - docker run lakhanramnani/docker-react set CI=true && npm run test -- --coverage 